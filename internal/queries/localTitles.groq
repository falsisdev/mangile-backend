{
  "data": *[
    (_type == "manga" || _type == "lightNovel")
    && ($type == "" || _type == $type)
    && ($tag == "" || $tag in tags)
    && ($status == "" || uploadStatus == $status)
    && ($search == "" || title match $search + "*")
  ] | order(
    select(
      $sort == "title_asc" => title asc,
      $sort == "title_desc" => title desc,
      $sort == "date_asc" => _createdAt asc,
      _createdAt desc
    )
  ) [$start..$end] {
    _id,
    _type,
    _createdAt,
    _updatedAt,
    title,
    "slug": slug.current,
    myAnimeListId,
    description,
    uploadStatus,
    tags,
    "coverImage": coverImage.asset->url,
    "bannerImage": bannerImage.asset->url
  },
  "total": count(*[
    (_type == "manga" || _type == "lightNovel")
    && ($type == "" || _type == $type)
    && ($tag == "" || $tag in tags)
    && ($status == "" || uploadStatus == $status)
    && ($search == "" || title match $search + "*")
  ])
}
